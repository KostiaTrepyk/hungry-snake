# 🐍 Hungry Snake (C++ Console)

Консольная реализация классической игры "Змейка" на чистом C++ с использованием Windows API для управления вводом и выводом.

## 🎯 Features

* **Render:** Консольный рендеринг через `system("cls")` и `cout`.
* **Скелет:** Использование `std::deque` для управления телом змейки (оптимально для добавления/удаления с обоих концов).
* **Логика:** Обработка столкновений с границами и самой собой.
* **Очки:** Показывает набранные очки.
* **Динамика:** Ускорение игры по мере роста.

## ⚠️ Критическая Зависимость (Dependencies)

**Внимание:** Этот код является **платформозависимым** (Platform-Dependent). Он требует прямого доступа к Windows API.

* **Компилятор:** **Microsoft Visual C++** (MSVC) или его аналог (для корректной работы `windows.h` и `conio.h`).
* **ОС:** Windows (Linux/macOS не поддерживаются из-за прямого вызова `windows.h`, `system("cls")` и `_kbhit()`).

## ⚙️ Запуск

Для запуска требуется IDE, настроенная на компиляцию C++.

1.  **Клонирование:** Получите исходный код.
    ```bash
    git clone [https://github.com/KostiaTrepyk/hungry-snake.git](https://github.com/KostiaTrepyk/hungry-snake.git)
    ```
2.  **Открытие:** Откройте файл решения **`Project1.slnx`** в **Visual Studio**.
3.  **Сборка и Запуск:** Используйте команду **Build & Run** (F5) в Visual Studio. Внешние библиотеки не требуются.

## 🕹️ Правила Игры (Gameplay)

* **Управление:** W, A, S, D.
* **Цель:** Собрать максимальное количество `*` (еды), не врезавшись в границы (`|` или `-`) или в собственный хвост.
